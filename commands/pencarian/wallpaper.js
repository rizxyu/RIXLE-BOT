module.exports = {name: ["wallpaper"],type: ['searching'],description: "searching wallpaper image",utilisation: global.userbot.prefix+ "wallpaper",async execute(m) {let {conn, text} = dataif (!text) return m.reply("query?")const json = await require("../../Lib/scrape").wallpapper(text)conn.sendFile(m.chat, json[Math.floor(Math.random () * json.length)], "", null, m)}}