module.exports = {name: ["google"],type: ['searching'],description: "searching google",utilisation: global.userbot.prefix+ "google",async execute(m) {let {conn, text} = dataif (!text) return m.reply("query?")teks = textm.reply("await process.....")res = await Ft.ggs({'query' : `${teks}`})raw = ``for (let i of res) {raw += `*Judul* : ${i.title}*Link* : ${i.link}*Keterangan* : ${i.snippet}`}const end = raw.trim()m.reply(end)}}