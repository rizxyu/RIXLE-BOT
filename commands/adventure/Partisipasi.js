/*
Masih Otak Atik

const db = require('../../db/peserta.json')//au
const event = require('../../event.json')

if (db = m.sender) return m.reply ('anda belum ikut partisipasi adventure dungeon silahkan daftar terlebih dahulu')

m.reply(`
Total peserta/partisipasi Adventure,dungeon💠
${db.length} player

🎪Event 
${event.log == '' ? 'tidak ada event yang tersedia' || event.log}
`)


*/
