const fetch = require('node-fetch')
module.exports = {
name: ["donasi"],
type: ["default"],
description: "no Comment",
utilisation: userbot.prefix + "extra",

async execute(m) {
let { conn } = data

} 
}
